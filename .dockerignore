# Only the Dockerfile, index.html, nginx-default.conf and docker-entrypoint.d/
# are needed as build context; keep everything else out so docs never bust the
# runtime layer.
.git
.gitignore
*.md
docker-compose.yml
disks/
